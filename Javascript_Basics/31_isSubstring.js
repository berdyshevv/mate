/**
 * @param {string} phrase
 * @param {string} part
 *
 * @returns {boolean}
 */
function isSubstring(phrase, part) {
    return phrase.includes(part);
  }
 
const isSubstring = (phrase, part) => {
    return phrase.includes(part);
  }